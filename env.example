PORT=3001

# Option 1: Use full DATABASE_URL (recommended for Render)
# Just paste your Internal Database URL from Render here:
DATABASE_URL=postgresql://user:password@host:port/database

# Option 2: Use individual env vars (alternative)
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=finance_survey
# PGUSER=postgres
# PGPASSWORD=postgres

PGPOOL_MAX=10
PG_IDLE_TIMEOUT_MS=30000
# Optional: limit CORS
# CORS_ORIGIN=https://your-landing-domain.com

